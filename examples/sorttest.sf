include "stdlib/io.sf";
include "stdlib/std.sf";
include "stdlib/sort.sf";

global readonly auto randData = proc(int n) {
	auto buf = new int[n];
	forallIndicies<int>(buf, proc(array<int> buf, int i) buf[i] = foreign[10] % 1000;);
	return buf;
};

auto myData = randData(230000);

quicksort<int>(myData, proc(int a, int b) => a - b);
$forall<int>(myData, proc(int i) println(itos(i)););